# `src/ui/` — presentation only

React components for input sections (§3), results panel + value-stack chart (§6),
the time-series schedule table (§6A), and the sensitivity/tornado + warnings.
Components render the arrays produced by `src/engine/compute.ts`; they never compute
financial quantities themselves.
