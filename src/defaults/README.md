# `src/defaults/` — validated 2026 defaults (PRD §5)

Defaults keyed by `geography × assetType × acquisitionType`, every value with a source
note and the 2026 vintage. Built in the "Domain types + validated defaults loader" issue.
